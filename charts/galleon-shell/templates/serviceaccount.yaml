{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "galleon-shell.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "galleon-shell.labels" . | nindent 4 }}
{{- end }}
